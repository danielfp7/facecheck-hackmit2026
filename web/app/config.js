// Where the FaceCheck server is, for a copy of these pages hosted somewhere else.
// Leave empty when the server serves these files itself.
// A Cloudflare quick tunnel changes URL every restart, so ?api=https://... overrides this
// and is remembered in the browser.
window.FACECHECK_API = "";
